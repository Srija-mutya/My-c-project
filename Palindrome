#include <stdio.h>
void main()
{
int num,a,b=0,rem;
printf("enter the number:");
scanf("%d",&num);
a=num;
while(num!=0)
{
    rem=num%10;
    b=(b*10)+rem;
    num=num/10;
}
if(a==b)
{
    printf("%d is a palindrome",a);
}
else
{
    printf("%d is not a palindrome",a);
}
}
